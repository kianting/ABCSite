<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title %>
  </div>

  <div class="field">
    <%= form.label :blog %>
    <%= form.text_area :blog, id: :blog_blog %>
  </div>

  <div class="field">
    <%= form.label :entry %>
    <%= form.datetime_select :entry, id: :blog_entry %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
